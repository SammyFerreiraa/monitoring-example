import { RegisterPage } from "@/templates"

const page = () => {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <RegisterPage />
    </main>
  )
}

export default page