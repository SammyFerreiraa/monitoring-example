import { LoginPage } from '@/templates'

const page = () => {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <LoginPage />
    </main>
  )
}

export default page
